# Extra dependencies for live trading only. Paper mode needs none of these.
#   pip install -r requirements.txt -r requirements-live.txt
solders>=0.21     # Solana transaction signing
base58>=2.1       # Phantom-style secret key decoding
